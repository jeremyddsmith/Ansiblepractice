---
- name: Create Nodes and Bucket
  hosts: tags_Name_jd*
  gather_facts: true
  become: true
  vars_files:
    - "vars.yaml"

  roles:
    - first_role
    - second_role
    - node_create
    - bucket_create
    - send_logs
